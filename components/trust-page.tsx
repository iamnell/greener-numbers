import { SiteFooter, SiteHeader } from "./site-header"; import { Breadcrumbs } from "./platform";
export function TrustPage({title,eyebrow,intro,sections}:{title:string;eyebrow:string;intro:string;sections:readonly (readonly [string,string])[]}){return <><SiteHeader/><main id="main-content" className="platform-main"><Breadcrumbs items={[{label:"Home",href:"/"},{label:title}]}/><section className="page-hero"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{intro}</p></section><section className="content-sections">{sections.map(([heading,copy])=><section key={heading}><h2>{heading}</h2><p>{copy}</p></section>)}</section></main><SiteFooter/></>;}
